# 🛠️ 开发脚本说明

## 快速使用

### 1. 重新编译并安装插件
```bash
# 方式1：使用脚本
./scripts/rebuild.sh

# 方式2：使用 npm
npm run rebuild
```

### 2. 仅打包（不安装）
```bash
npm run package
```

### 3. 开发模式（自动监听文件变化）
```bash
./scripts/dev.sh
```

---

## 📋 脚本详情

### `rebuild.sh` - 重新编译安装
**功能：**
- 卸载旧版本插件
- 清理旧的 .vsix 文件
- 重新打包插件
- 自动安装到 VSCode

**使用场景：**
- 修改代码后需要测试
- 快速迭代开发

**执行：**
```bash
./scripts/rebuild.sh
# 或
npm run rebuild
```

---

### `dev.sh` - 开发模式（可选）
**功能：**
- 监听 `src/` 目录文件变化
- 自动触发重新编译安装

**前置条件：**
```bash
# 需要安装 inotify-tools
sudo apt-get install inotify-tools
```

**使用场景：**
- 频繁修改代码
- 需要实时看到效果

**执行：**
```bash
./scripts/dev.sh
```

---

## 🔄 开发工作流

### 标准流程
```bash
1. 修改代码
2. 运行: ./scripts/rebuild.sh
3. 重新加载 VSCode 窗口 (Ctrl+Shift+P → Reload Window)
4. 测试功能
5. 重复 1-4
```

### 快速流程（使用开发模式）
```bash
1. 运行: ./scripts/dev.sh
2. 修改代码（自动触发重新编译）
3. 重新加载 VSCode 窗口
4. 测试功能
```

---

## 📦 npm scripts

在 `package.json` 中定义的脚本：

| 命令 | 说明 |
|------|------|
| `npm run package` | 仅打包插件 |
| `npm run rebuild` | 重新编译并安装 |
| `npm run install-local` | 安装最新的 .vsix 文件 |

---

## 💡 提示

- ⚠️ 每次安装后需要**重新加载 VSCode 窗口**才能生效
- 🔧 开发时建议使用 `rebuild.sh` 脚本
- 📦 发布前使用 `vsce package` 确保打包正确
